[project]
name = "imageslime"
version = "0.1.0"
description = "Interactive image segmentation and editing tool using SAM3"
requires-python = ">=3.10"

[dependency-groups]
dev = [
    "pytest>=7.4.0",
    "black>=23.0.0",
    "ruff>=0.1.0",
]

[tool]
# Use the project's pyproject.toml for dependencies
# This allows uv to manage the project dependencies
